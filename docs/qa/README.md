# QA snapshots

- `FEATURE_STORIES.csv` — frozen output of the 2026-06 manual user-story
  audit (122 stories, all passing at the time). It is **not maintained**;
  current behavior is guarded by the test suites (`make ci`). Kept for
  historical context on what was hand-verified and which defects were
  found and fixed.
