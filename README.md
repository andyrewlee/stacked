# stacked

A minimal, **login-free** CLI for managing **stacked diffs** on top of plain `git`.
The command is `st`.

Pure Go, standard library only — it shells out to your system `git` and stores stack
metadata locally inside `.git`.
